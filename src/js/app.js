{
  const addToCart = () => {
    items.add(item);
  }

  const addToCartHandler = () => {
    //..
    try {
      test + cos
      addToCart();
    } catch (error) {
      // alert('Coś poszło nie tak!');
    }
    //..
    console.log('odświeżenie widoku');
  }

  addToCartHandler();
}